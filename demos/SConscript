import os
import sys

env=DefaultEnvironment().Clone()
BIN_DIR=os.environ['BIN_DIR'];
LIB_DIR=os.environ['LIB_DIR'];

env.Library(os.path.join(LIB_DIR, 'main'), ['demo_main.c'])

env['LIBS'] = ['main'] + env['LIBS']
env.Program(os.path.join(BIN_DIR, 'demo_label'), ['label.c'])




